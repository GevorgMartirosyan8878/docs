# Redux

## Redux terminology

### Action

Action is the object, with type property

### Action creator

Action creator is the function which returns the action 

### Reducers

Reducer is the function that accepts state and action as arguments,
and return the new state of the application

Simply view of reducer: (prevState, action) => newState
